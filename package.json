{
  "name": "tray-helper",
  "version": "0.2.0",
  "private": true,
  "homepage": "./",
  "author": "gustavo <gustavoferri13@gmail.com>",
  "main": "public/electron/main.js",
  "dependencies": {
    "@material-ui/core": "^4.11.0",
    "@material-ui/icons": "^4.9.1",
    "electron-store": "^5.2.0",
    "react": "^16.13.1",
    "react-dom": "^16.13.1",
    "react-router-dom": "^5.2.0",
    "react-scripts": "3.4.1"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "electron": "electron .",
    "pack:linux": "electron-builder build -l -c.extraMetadata.main=build/electron/main.js --publish never",
    "pack:win": "electron-builder build -w -c.extraMetadata.main=build/electron/main.js --publish never"
  },
  "build": {
    "asar": false,
    "files": [
      "build/**/*",
      "node_modules/**/*"
    ],
    "linux": {
      "target": [
        {
          "target": "deb",
          "arch": [
            "x64",
            "ia32"
          ]
        }
      ]
    }
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "devDependencies": {
    "electron": "^9.0.5",
    "electron-builder": "^22.7.0"
  }
}
